font-family = JetBrains Mono

# The checkbox in the update window doesn't actually do anything
# By default, this is set to always check
auto-update = off

keybind = global:ctrl+grave_accent=toggle_quick_terminal
